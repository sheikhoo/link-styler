export interface Setting {
  bg: boolean;
  bgColor: string;
  textColor: string;
  borderRadius: number;
  showIcon: boolean;
  iconColor: boolean;
  pathnameLengthLimit: number;
  atsignPath: string;
  hashPath: string;
}
